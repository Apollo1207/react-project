import styled from "styled-components";
import {Field} from "formik";

export const TextField = styled(Field)`
  width: 25%;
  padding: 10px;
  border-radius: 10px;
  font-size: 16px;
  margin-top: 25px;
`;